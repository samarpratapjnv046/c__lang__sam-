#include<stdio.h>
#include<conio.h>
int main(){
    // char ch();
    // ch= getch();
    // ch=getchar();
    // ch=getche();
    //  for gets function ewe use char ch[10]
    char ch[10];
    gets(ch);
    // printf("ch=%c",ch);
    printf("ch=%s",ch);
    return 0;
}