#include<stdio.h>
#include<conio.h>
void main(){
    // char ch;
    char ch[20];
    printf("enter a character\n");
    // getchr();
    gets(ch);
    // printf("\n character is:",ch);
    // putchar(ch); 
    // putch(ch);
    puts(ch);
    getch();
}